<span id="cart-count" hx-swap-oob="true"
      class="absolute -right-2.5 -top-2 grid h-4 min-w-4 place-items-center rounded-full bg-accent px-1 text-[10px] font-semibold text-white {{ '' if cart_count else 'hidden' }}">{{ cart_count }}</span>
